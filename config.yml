# ROM Version Configuration
# This file defines the ROM versions and their properties for the decompilation project

game:
  name: "Starrod"
  short_name: "PM64"

versions:
  us_rev0:
    region: "US"
    revision: 0
    sha1: "0000000000000000000000000000000000000000"
    rom_size: 0x00000000
    entry_point: 0x8000040000

paths:
  baserom: "baserom.z64"
  build_dir: "build"
  assets_dir: "assets"
  yaml_dir: "yamls/assets/yaml"

memory:
  ram_start: 0x80000000
  ram_size: 0x00800000  # 8MB

segments:
  # Define ROM segments here
  # boot:
  #   vram: 0x80000400
  #   files:
  #     - src/boot/...
